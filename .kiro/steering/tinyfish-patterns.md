---
inclusion: fileMatch
fileMatchPattern: "**/tinyfish/**"
---

# Kiro Mirror: TinyFish Patterns

This file is a thin mirror. See `.claude/playbooks/tinyfish-and-agent.md`.

Keep top of mind:

- MCP auth and REST auth are different
- financing scout is the hero live lane
- every TinyFish path must support mock, misconfigured, and live
- vendor and insurance may remain seeded during hybrid live mode
- `/api/tinyfish/health` is a mode/config probe
- `/api/tinyfish/demo-run` is the judge-facing contract
