{
  "name": "Secret Hygiene Check",
  "version": "1.0.0",
  "description": "Checks that no secrets or API keys are being committed to tracked files",
  "when": {
    "type": "preToolUse",
    "toolTypes": ["write"]
  },
  "then": {
    "type": "askAgent",
    "prompt": "Before writing: verify this file does NOT contain any hardcoded API keys, secrets, tokens, or credentials. TinyFish keys belong in .env.local only. If you see a real secret, remove it and use an environment variable reference instead."
  }
}
